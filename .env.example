# Copy to .env and fill in as needed

# OpenRouter API key (acts as OPENAI_API_KEY for OpenRouter endpoint)
OPENAI_API_KEY=...

# Parsing defaults
MAX_RETRIES=10
TEMPERATURE=0.2
TOP_P=0.9
SEED=

# Data/output locations
DATA_DIR=data
OUTPUTS_DIR=outputs
